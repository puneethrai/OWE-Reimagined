/*globals define*/
define(['text!./templates/Setting.html'], function () {
    return {
        templates: arguments,
        names: ['Setting'],
        templatePath: 'templates',
        moduleName: 'baseModule'
    };
});